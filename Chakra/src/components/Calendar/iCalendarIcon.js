import React from 'react';
import {ReactComponent as ReactLogo} from '../../icons/proto/iCalendarIcon.svg';

const calendarIcon = () => {

    return (
            <ReactLogo />
    );
}

export default calendarIcon;